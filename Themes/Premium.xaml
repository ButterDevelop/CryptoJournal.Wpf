<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:ui="clr-namespace:CryptoJournal.Wpf.UI">

    <!-- Global tokens -->
    <CornerRadius x:Key="AppCornerRadius">14</CornerRadius>
    <Thickness x:Key="AppControlPadding">14,10</Thickness>

    <Duration x:Key="AnimFast">0:0:0.08</Duration>
    <Duration x:Key="AnimNormal">0:0:0.12</Duration>
    <CubicEase x:Key="EaseOut" EasingMode="EaseOut"/>

    <!-- "neon" as resources (so as not to duplicate in each button) -->
    <RadialGradientBrush x:Key="NeonBorderBrush">
        <GradientStop Color="#FF28B8C1" Offset="0"/>
        <GradientStop Color="#FF04FCFC" Offset="1"/>
    </RadialGradientBrush>

    <RadialGradientBrush x:Key="NeonForegroundBrush">
        <GradientStop Color="#FF22B1C5" Offset="0"/>
        <GradientStop Color="#FF00E0FF" Offset="1"/>
    </RadialGradientBrush>

    <!-- extend the MaterialDesign style, not replace it -->
    <Style x:Key="Premium.TextBlockBase"
       TargetType="{x:Type TextBlock}"
       BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}"
       BasedOn="{StaticResource Premium.TextBlockBase}" />

    <!-- Buttons (default all) -->
    <Style TargetType="{x:Type Button}"
           BasedOn="{StaticResource MaterialDesignFlatButton}">
        <Setter Property="Padding" Value="{StaticResource AppControlPadding}"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius"
                Value="{StaticResource AppCornerRadius}"/>

        <!-- easy micro-hover via scale -->
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                             To="1.03" Duration="{StaticResource AnimFast}"
                                             EasingFunction="{StaticResource EaseOut}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1.03" Duration="{StaticResource AnimFast}"
                                             EasingFunction="{StaticResource EaseOut}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                             To="1" Duration="{StaticResource AnimNormal}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1" Duration="{StaticResource AnimNormal}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Opacity" Value="0.92"/>
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- "Neon" action buttons (dotted, but without copy-paste) -->
    <Style x:Key="NeonActionButton"
           TargetType="{x:Type Button}"
           BasedOn="{StaticResource MaterialDesignOutlinedButton}">
        <Setter Property="BorderBrush" Value="{StaticResource NeonBorderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource NeonForegroundBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>

        <!-- v5+ -->
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp8"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Card base (taking the real style key from 5.x) -->
    <Style x:Key="Premium.CardBase"
       TargetType="{x:Type materialDesign:Card}"
       BasedOn="{StaticResource MaterialDesignElevatedCard}">
        <Setter Property="UniformCornerRadius" Value="18"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Padding" Value="14"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
    </Style>

    <!-- Card (default all) -->
    <Style TargetType="{x:Type materialDesign:Card}"
           BasedOn="{StaticResource Premium.CardBase}">
        <Setter Property="UniformCornerRadius" Value="18"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Padding" Value="14"/>

        <!-- v5+ -->
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>

        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="0"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp6"/>
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                             To="-2" Duration="{StaticResource AnimFast}"
                                             EasingFunction="{StaticResource EaseOut}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                             To="0" Duration="{StaticResource AnimNormal}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGrid "smooth, beautiful, center" (default all) -->
    <Style x:Key="Premium.DataGridBase"
       TargetType="{x:Type DataGrid}"
       BasedOn="{StaticResource {x:Type DataGrid}}">
        <Setter Property="RowHeight" Value="36"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
    </Style>

    <Style TargetType="{x:Type DataGrid}"
       BasedOn="{StaticResource {x:Type DataGrid}}">
        <Setter Property="ui:DataGridCenteringAssist.Enable" Value="True"/>
    </Style>

    <Style TargetType="{x:Type DataGridColumnHeader}"
       BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
    </Style>

    <Style TargetType="{x:Type DataGridCell}"
       BasedOn="{StaticResource {x:Type DataGridCell}}">
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <!-- Removing focus frames in cells -->
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <!-- Removing visual highlighting for selection/hover for rows -->
    <Style TargetType="{x:Type DataGridRow}"
       BasedOn="{StaticResource {x:Type DataGridRow}}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Style.Triggers>
            <!-- Selected -->
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Trigger>

            <!-- Hover -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="Transparent"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>